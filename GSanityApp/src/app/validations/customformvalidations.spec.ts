import { CustomFormValidations } from './customformvalidations';

describe('DNIValidator', () => {
	it('should create an instance', () => {
		expect(new CustomFormValidations()).toBeTruthy();
	});
});
